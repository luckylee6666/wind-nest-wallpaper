{
  "name": "wind-nest-wallpaper",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:web": "esbuild web-src/app.js --bundle --minify --format=iife --target=safari16 --loader:.glb=dataurl --outfile=Resources/app.js"
  },
  "dependencies": {
    "three": "0.185.1"
  },
  "devDependencies": {
    "esbuild": "0.28.1"
  }
}
